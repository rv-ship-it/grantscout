# Optional: Set your Claude API key to enable AI-powered semantic scoring.
# Without this key, the tool uses keyword-only scoring (still useful!).
# Get a key at: https://console.anthropic.com/
CLAUDE_API_KEY=
